# Stable-channel rustfmt options only. Nightly-only options (imports_granularity,
# group_imports, ...) are silently ignored on stable — do not add them here.
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
