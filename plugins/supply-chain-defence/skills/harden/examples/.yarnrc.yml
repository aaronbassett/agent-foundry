# Supply chain defence — hardened yarn configuration
#
# Add npmMinimumReleaseAge to your .yarnrc.yml.
# Accepts duration strings: "5d", "1w", "120h", or plain minutes.
#
# This prevents yarn from resolving package versions published less than
# the specified duration ago.

npmMinimumReleaseAge: "5d"
